Input is the single-line text field; onboarding uses `big` for display-size entry.

```jsx
<Input label="სახელი" placeholder="შენი სახელი" big />
<Input label="ტელეფონი" error="ვერ გაიგზავნა. სცადე თავიდან." />
```
