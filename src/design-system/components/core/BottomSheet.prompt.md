BottomSheet is Guli's only modal surface — bg-1, 24px top radius, 36×4 grabber, scrim behind.

```jsx
<BottomSheet title="მეგობრის დამატება" onClose={close}>…</BottomSheet>
```

Position it inside the 390×844 app frame (it is absolutely positioned to its nearest relative ancestor).
