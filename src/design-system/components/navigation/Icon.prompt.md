Icon inlines Lucide glyphs at Guli's 1.75px rounded stroke (stand-in for the future custom set).

```jsx
<Icon name="droplet" size={24} color="var(--saperavi-tint)" />
```

Canonical assignments — tabs: დღეს=sun, რუკა=map, წრე=users, მე=circle-user. Traits: E=sun-medium, A=blend, C=layers, N=waves, O=sunrise. Needs network (unpkg).
