ThisOrThatPair renders the forced-choice question format — two cards side by side (or `stacked` for long strings).

```jsx
<ThisOrThatPair optionA="დიდი ხმაურიანი სუფრა" optionB="ორი ძველი მეგობარი"
  selected={sel} onSelect={setSel} />
```
